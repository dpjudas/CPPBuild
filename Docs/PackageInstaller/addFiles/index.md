# PackageInstaller: addFiles() method

**`PackageInstaller.addFiles()`** adds files to the package.

## Syntax

```js
addFiles(files)
```

### Parameters

- `files`

    An array of files. Each file is either a string with a path, or an object with two members: `src` and `dest`.

### Return value

None.
