# Environment: isUnix() static method

The **`isUnix()`** static method of the `Environment` object
returns true if CPPBuild is running under a posix compatible OS.

## Syntax

```js
Environment.isUnix()
```

### Parameters

None.

### Return value

A boolean.

## Description

If CPPBuild is running on a posix compatible operating system this will return true.
